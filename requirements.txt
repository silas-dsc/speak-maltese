fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27

# Key-less Maltese TTS: Edge Read-Aloud exposes the same mt-MT-GraceNeural /
# mt-MT-JosephNeural voices as Azure. Unofficial endpoint — fine for personal
# study, use a real AZURE_SPEECH_KEY for anything else.
edge-tts>=6.1

# Local, key-less Maltese STT.
#   - wav2vec2 (torch + transformers) is the default and the fast path: a CTC
#     model runs on Apple Silicon's GPU in ~0.1s an utterance.
#   - faster-whisper is the fallback, and still needed for audio decoding.
faster-whisper>=1.0
torch>=2.2
transformers>=4.44

# Fully offline Maltese TTS (facebook/mms-tts-mlt) — opt in with SM_TTS_PROVIDER=mms.
# It needs no extra packages beyond torch/transformers above.
